#include <stdio.h>

int main() {

    int num1 = 10;
    int num2 = 20;

    if (&num1 > &num2) {
        printf("OO primeiro endereço (%p) e maior.\n", (void*)&num1);
    } else if (&num1 < &num2) {
        printf("O segundo endereco (%p) é maior.\n", (void*)&num2);
    } else {
        printf("Os enderecos sao iguais.\n");
    }

    return 0;
}
